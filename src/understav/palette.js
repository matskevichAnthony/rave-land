/**
 * Палитра и бюджет кадра сцены UNDERSTAV.
 *
 * Один файл на всю сцену намеренно: архитектура, типографика и постобработка обязаны
 * светить одними и теми же цветами, иначе кадр рассыпается на три разных картинки.
 */

export const PALETTE = {
  void: '#05050a',
  iron: '#14151b',
  concrete: '#2a2b33',
  rust: '#5c2a15',
  bone: '#cbc2b2',
  ember: '#ff4d0d',
  flame: '#ffe0c4',
  emberHalo: '#ffa04a',
  trip: '#9b30ff',
  blood: '#ff1f2e',
  moon: '#8fa8ff',
};

/**
 * Бюджет кадра. Сцену снимают с экрана, поэтому что провисло, то и попало в видео:
 * числа стоят в задании до кода, а не подгоняются после.
 */
export const FRAME_BUDGET = {
  // Потолок поднят с 140 осознанно и дважды: сцена отрастила стометровый коридор, а мягкие
  // частицы стоят второго обхода непрозрачной геометрии (проход глубины, около шестидесяти
  // вызовов). На видеокарте это дёшево, но в счётчике вызовов видно как половину кадра.
  drawCalls: 200,
  triangles: 400000,
  lights: 6,
  targetFps: 60,
};

/** Темп ритуала: пульс материалов и света берётся отсюда, а не из констант по модулям. */
export const BEAT = {
  bpm: 132,
  get seconds() {
    return 60 / this.bpm;
  },
};
